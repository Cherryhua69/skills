interface:
  display_name: "Remote Device Coding"
  short_description: "Safely edit and verify code on LAN servers over SSH"
  default_prompt: "Use $remote-device-coding to modify code on a LAN server without touching the local project."
